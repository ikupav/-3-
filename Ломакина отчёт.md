# отчёт Практические задания по Эконометрике на компьютере (2 курс, 3 семестр)

# 1. LibreOffice (или Яндекс Таблицы).
Случайные числа, гистограмма, описательная статистика
Что сделано, рассчитано

1. Сформированы исходные данные:
   · Заданы параметры нормального распределения: среднее значение (μ = 10) и стандартное отклонение (σ = 4).
   · С использованием функции =_xlfn.NORM.INV(RAND(); μ; σ) сгенерирована выборка из 1000 нормально распределенных случайных чисел.
2. Определены параметры группировки данных:
   · Задана ширина интервала гистограммы: ≈ 2.006.
   · Подготовлена таблица для группировки данных (указаны номера интервалов и начальные границы).
3. Подготовлена структура гистограммы:
   · Созданы столбцы для:
     · № интервала
     · Начало интервала
     · Частота (количество значений, попадающих в каждый интервал).
   · Выделено 15 интервалов для визуализации распределения.
4. Реализована возможность обновления данных:
   · При каждом пересчете листа (например, при нажатии F9) генерируется новая случайная выборка, что позволяет наблюдать изменение гистограммы.

---

Вывод по работе

В результате выполнения работы была смоделирована выборка из 1000 значений, распределенных по нормальному закону с заданными параметрами (μ = 10, σ = 4). Подготовлена структура для построения гистограммы, которая позволяет наглядно оценить соответствие эмпирического распределения теоретическому. Гистограмма, построенная на основе рассчитанных частот, визуализирует форму нормального распределения, демонстрируя концентрацию данных около среднего значения и симметричность распределения. Работа может быть использована для изучения свойств нормального распределения и методов визуализации статистических данных.

Полученные результаты демонстрируют практическое применение методов генерации случайных данных и их визуализации в Excel.

Скриншот работы
![IMAGE ALT](<img width="1091" height="363" alt="image" src="https://github.com/user-attachments/assets/903df690-0615-4d58-86a0-784ddb2bdf7b" />

# 2. LibreOffice (или Яндекс Таблицы).
Парная корреляция [1, Лабораторная работа 1, с. 8 - 24].
Что сделано, рассчитано

1. Собраны и систематизированы исходные данные:
   · Даны два ряда данных: переменная X (независимая) и переменная Y (зависимая), по 40 наблюдений.
2. Рассчитаны вспомогательные показатели:
   · Квадраты значений $$x^2$$ и $$y^2.$$
   · Произведение xy для каждой пары значений.
   · Теоретические значения \hat{y}, рассчитанные по уравнению регрессии вида:
     \$$hat{y} = 15 + \frac{85}{x}$$
   
4. Вычислены суммы и средние значения:
   · Суммы и средние арифметические для X, Y, x^2, y^2, xy, \hat{y}, а также для квадратов отклонений.
5. Построены показатели отклонений:
   · Квадраты отклонений теоретических значений от фактических: (\hat{y} - y)^2.
   · Квадраты отклонений фактических значений от среднего Y=y - \bar{y})^2.

---

Вывод по работе

В ходе работы была проведена подготовка данных для анализа парной корреляционной зависимости между переменными X и Y. Рассчитаны необходимые суммы и средние значения, а также построены теоретические значения \hat{y} по заданному уравнению регрессии. Полученные данные позволяют в дальнейшем вычислить коэффициент корреляции, оценить тесноту связи между переменными и проверить адекватность предложенной регрессионной модели. Работа демонстрирует применение табличного процессора для статистической обработки данных и подготовки к регрессионному анализу.

Работа демонстрирует подготовительный этап корреляционно-регрессионного анализа.

Скриншот работы![IMAGE ALT](<img width="1080" height="727" alt="Снимок экрана 2025-12-16 020036" src="https://github.com/user-attachments/assets/bb4caa70-8c21-4372-9e60-1249c4335d25" />


# 3. LibreOffice (или Яндекс Таблицы).
Парная регрессия [1, Лабораторная работа 2, с. 24 - 31].

то сделано, рассчитано:
Собраны и систематизированы парные данные по переменным x и y для 45 наблюдений. Построены два варианта регрессионных моделей:

Линейная регрессия после логарифмирования xx: модель вида y^=−200+70⋅ln.Логарифмически преобразованные переменные: рассчитаны ln(x), ln(y), их квадраты и произведения. Рассчитаны вспомогательные показатели для анализа модели:
x2, y2, xy (для исходных данных).$$ln2(x), ln2(y), ln(x)⋅ln(y) (для логарифмированных данных).$$
Оценены ошибки модели:
-Абсолютная относительная ошибка ∣(y^−y) : y∣,

-Квадраты ошибок (y^−y)2,

-Квадраты отклонений фактических значений от среднего (y−yˉ)2.
Вычислены суммы и средние значения по всем столбцам для дальнейших статистических расчётов (например, для расчёта коэффициента детерминации R2, стандартной ошибки и тестов значимости).

Вывод по работе:

В работе проведён сравнительный анализ двух подходов к моделированию парной регрессии:

-Модель с логарифмированием независимой переменной =y^=−200+70⋅ln(x),

-Модель с полностью логарифмированными переменными (подготовка для потенциальной степенной или линейной регрессии в логарифмах).

Рассчитанные ошибки и отклонения позволяют оценить качество подгонки модели и сравнить её с вариацией исходных данных. Наличие сумм квадратов ошибок и отклонений создаёт основу для расчёта коэффициента детерминации R2 и проведения тестов на адекватность модели.
Работа демонстрирует практический подход к выбору и оценке регрессионной модели при наличии нелинейной связи между переменными, а также важность преобразования данных для улучшения качества модели.

Скриншот работы [IMAGE ALT](<img width="894" height="654" alt="Снимок экрана 2025-12-16 020404" src="https://github.com/user-attachments/assets/e3fa54b4-ac82-410b-b122-74c19367c62e" />


# 4. Практ задание по множественной регрессии
[2, Раздел 2, с.45-89].
Ознакомиться с образцом и сделать свой вариант.

Подготовиться отвечать на контрольные вопросы.

Что сделано, рассчитано

1. Подготовка исходных данных:
   · Даны два ряда данных: переменная X (независимая) и Y (зависимая), по 12 наблюдений.
   · Значения X и Y охватывают достаточно широкий диапазон, что позволяет оценить нелинейные связи.
2. Логарифмическое преобразование переменных:
   · Для каждой пары (X, Y) вычислены:
     · \ln X
     · \ln Y
     · 1 / X
   · Эти преобразования необходимы для линеаризации трёх типов моделей:
     · Степенная модель: y = a \cdot x^b → линеаризуется как \ln y = \ln a + b \cdot \ln x.
     · Равносторонняя гипербола: y = a + b/x → линеаризуется как замена z = 1/x.
     · Показательная модель: y = a \cdot e^{b x} → линеаризуется как \ln y = \ln a + b \cdot x.
3. Расчёт сумм и средних значений:
   · Для каждой преобразованной переменной (\ln X, \ln Y, 1/X) рассчитаны сумма и среднее значение.
   · Эти величины будут использоваться для нахождения параметров моделей методом наименьших квадратов.

---

Вывод по работе

В ходе работы были подготовлены данные и выполнены необходимые преобразования для построения трёх типов нелинейных регрессионных моделей: степенной, гиперболической и показательной. Логарифмирование и взятие обратных значений позволили свести нелинейные зависимости к линейным, что упрощает оценку параметров моделей с помощью метода наименьших квадратов.

Полученные суммы и средние значения преобразованных переменных служат основой для последующего расчёта коэффициентов регрессии, оценки качества моделей и выбора наилучшей зависимости между X и Y. Работа демонстрирует важность предварительного преобразования данных при анализе нелинейных связей в статистике.
Скриншот работы [IMAGE ALT]<img width="1066" height="239" alt="image" src="https://github.com/user-attachments/assets/71d07c3b-c66a-424d-85b3-935ac6445658" />

# 5. Практич.задание по нарушению классической модели
множественной регрессии [2, Раздел 3, с.80-106].

Что сделано, рассчитано

1. Построена линейная регрессионная модель:
   · На основе данных X и Y (10 наблюдений) рассчитаны параметры модели Y = a + bX + \varepsilon.
   · Найдены средние значения: \bar{x} = 38.8, \bar{y} = 22.6.
   · Рассчитаны коэффициенты:
     · $$b = \frac{\sum (x_i - \bar{x})(y_i - \bar{y})}{\sum (x_i - \bar{x})^2} \approx 0.3454$$
     · $$a = \bar{y} - b\bar{x} \approx 9.198$$
   · Уравнение регрессии: \hat{Y} = 9.198 + 0.3454X.
2. Вычислены остатки модели:
   · Для каждого наблюдения найдены остатки e_i = y_i - \hat{y}_i.
3. Проверка на гетероскедастичность (непостоянство дисперсии остатков):
   · Тест Гольдфельда–Квандта:
     · Данные упорядочены по X, исключены 4 центральных наблюдения.
     · Рассчитаны суммы квадратов остатков для двух групп: RSS_1 = 11.825, RSS_2 = 41.877.
     · Рассчитана статистика F = 3 и сделан вывод об отсутствии гетероскедастичности по данному тесту.
   · Тест Спирмена:
     · Ранжированы данные по |e_i| и X, вычислен коэффициент ранговой корреляции r_s.
     · Полученное значение t-статистики t \approx 2.487 превышает критическое значение t_{0.025;8} = 2.306, что указывает на наличие гетероскедастичности.
   · Тест Уайта (предложен, но не полностью реализован):
     · Указана необходимость построения регрессии e_i^2 = a_0 + a_1 x_i + a_2 x_i^2 + V_i для проверки.

---

Вывод по работе

В ходе работы была построена линейная регрессионная модель, связывающая переменные X и Y. Проведена комплексная проверка остатков модели на гетероскедастичность с использованием нескольких тестов. Результаты тестов противоречивы: тест Гольдфельда–Квандта не выявил гетероскедастичности, тогда как тест Спирмена показал её наличие. Это указывает на необходимость дальнейшего анализа, возможно, с применением теста Уайта или других методов, а также на возможность использования робастных стандартных ошибок или преобразования данных для коррекции модели. Работа демонстрирует важность проверки предпосылок регрессионного анализа для обеспечения достоверности результатов.

Скриншот работы [IMAGE ALT]<img width="765" height="487" alt="Снимок экрана 2025-12-16 020918" src="https://github.com/user-attachments/assets/8b57d0b0-c0ee-4bc0-a71e-80982ccbc278" />


# 6. Нелинейная регрессия и способы реализации
[2, Раздел 4, с.107-140].
Что сделано, рассчитано

1. Подготовка исходных данных:
   · Представлены два ряда данных: независимая переменная X и зависимая переменная Y, каждый по 12 наблюдений.
   · Значения X варьируются от 4.2 до 26.7, а Y — от 1.2 до 4.9, что позволяет исследовать возможные нелинейные зависимости.
2. Преобразование переменных для линеаризации моделей:
   · Для каждого наблюдения вычислены:
     · Натуральный логарифм X (\ln x)
     · Натуральный логарифм Y (\ln y)
     · Обратная величина X (1/x)
   · Эти преобразования соответствуют трём типам нелинейных моделей:
     · Степенная модель: y = a \cdot x^b → линеаризуется как \$$ln y = \ln a + b \cdot \ln x
     · Равносторонняя гипербола: y = a + \frac{b}{x} → линеаризуется заменой z = 1/x
     · Показательная модель: y = a \cdot e^{bx} → линеаризуется как \ln y = \ln a + b \cdot x
3. Расчёт сводных статистик:
   · Для каждой исходной и преобразованной переменной рассчитаны:
     · Сумма значений
     · Среднее значение
   · Статистики вычислены как для исходных X и Y, так и для \ln X, \ln Y, 1/X.
4. Обнаружена возможная ошибка в данных:
   · В строке 12 значение X = 147.1 резко выделяется на фоне остальных (от 4.2 до 26.7), что может быть опечаткой или выбросом, требующим проверки.

---

Вывод по работе

В ходе работы выполнена подготовка данных для построения и сравнения трёх типов нелинейных регрессионных моделей. Путем логарифмирования и взятия обратных значений переменные преобразованы к линейному виду, что позволяет применять метод наименьших квадратов для оценки параметров моделей.

Ключевые результаты:

· Получены суммы и средние значения всех необходимых переменных для последующего расчёта коэффициентов регрессии.
· Выявлен возможный выброс в данных (X = 147.1), который может существенно влиять на результаты моделирования и требует дополнительной проверки.

Скриншот работы
[IMAGE ALT]<img width="1803" height="455" alt="Снимок экрана 2025-12-16 021020" src="https://github.com/user-attachments/assets/8751308b-7a66-4714-b378-0c449114aacb" />

# 7. Анализ данных в R [3, общее задание и задание по вариантам].
Отчет по анализу данных mtcars
Что сделано, рассчитано

1. Загрузка и обзор данных:
   · Использован встроенный набор данных mtcars, содержащий технические характеристики 32 автомобилей.
   · Просмотрена структура данных (str()), сводная статистика (summary()), первые строки (head()).
   · Определено количество наблюдений (32 строки) и переменных (11 столбцов).
2. Описательный анализ переменной mpg (миль на галлон):
   · Рассчитаны основные статистики:
     · Среднее значение: ~20.1 миль на галлон
     · Медиана: 19.2
     · Стандартное отклонение: ~6.0
     · Минимум: 10.4, максимум: 33.9
     · Первый квартиль (Q1): 15.43, третий квартиль (Q3): 22.80
   · Интерпретированы результаты, указывающие на разброс и центральную тенденцию данных.
3. Создание новой переменной kpl (километров на литр):
   · Переменная mpg преобразована в kpl (1 миля на галлон ≈ 0.425 км на литр).
4. Построение гистограммы для мощности двигателя (hp):
   · Гистограмма с 10 интервалами показывает распределение мощности.
   · Добавлена вертикальная линия среднего значения (~146.7 л.с.).
   · Отмечена правосторонняя асимметрия: большинство значений в диапазоне 50–150 л.с., есть выбросы >200 л.с.
5. Анализ зависимости расхода топлива от веса автомобиля:
   · Построен диаграмма рассеяния mpg от wt (вес в тысячах фунтов).
   · Добавлена линия линейной регрессии (lm(mpg ~ wt)).
   · Рассчитан коэффициент корреляции Пирсона: r ≈ –0.87, что указывает на сильную отрицательную связь.

---

Вывод по работе

Проведён комплексный описательный анализ набора данных mtcars, включающий расчёт статистик, преобразование переменных и визуализацию. Установлено, что:

· Расход топлива (mpg) имеет среднее значение около 20 миль на галлон с умеренным разбросом.
· Мощность двигателя (hp) распределена асимметрично: большинство автомобилей обладают мощностью 50–150 л.с.
· Вес автомобиля (wt) сильно отрицательно коррелирует с расходом топлива: чем тяжелее автомобиль, тем меньше миль на галлон он проезжает.

Результаты анализа демонстрируют эффективность использования R для первичного исследования данных, визуализации зависимостей и интерпретации статистических показателей. Полученные выводы могут служить основой для дальнейшего моделирования, например, для прогнозирования расхода топлива на основе технических характеристик.
Скриншот работы
[IMAGE ALT]<<img width="636" height="915" alt="Снимок экрана 2025-12-16 021149" src="https://github.com/user-attachments/assets/178cc71f-9fa5-4e7d-92fe-255133f8b13d" />




